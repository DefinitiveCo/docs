---
title: AI Integration
description: Everything an AI agent needs to start trading with the Flash API.
---

# AI Integration

Machine-readable references:

- [`/llms.txt`](/llms.txt) — index of doc pages with links. Best for agents that can fetch URLs.
- [`/llms-full.txt`](/llms-full.txt) — all docs concatenated into one file. Best for pasting into a prompt or uploading as a single context file.
- [`/openapi.json`](https://ddp.definitive.fi/v2/flash/openapi.json) — OpenAPI spec.

## Integrate with an LLM

1. **Get an API key** — follow [Become an Integrator](/getting-started).
2. **Give your LLM the docs** — point it at [`/llms.txt`](/llms.txt) or paste [`/llms-full.txt`](/llms-full.txt) into its context.
3. **Call the API** — `POST /quote`, sign the returned payload, then `POST /order` (details below).

## Auth

Pass an API key in the `x-definitive-api-key` header on every request. If you only want to trade, you can use the prefilled Definitive integrator key:

```
dpka_b8f185b6_0653_49ff_b683_048ec821a2b8
```

To register your own org as the integrator, follow [Become an Integrator](/getting-started).

## Flow

Base URL: `https://ddp.definitive.fi/v2/flash`

Three steps: quote → sign → submit. The walkthrough below covers EVM chains.

### 1. Get a quote

`POST /quote` — returns pricing for a swap. Pass `targetAsset` / `contraAsset` (with matching `targetChain` / `contraChain` — same-chain only), `side` (`buy` or `sell`), `qty` as a decimal string in the spent asset's units (`contraAsset` units on a `buy`, `targetAsset` units on a `sell`), and `orderType`.

<Note>
Native assets cannot be swapped directly. Wrap them into their tokenized equivalent first (for example WETH on EVM chains, or WSOL on Solana), then reference that token by address.
</Note>

Returns:

- `quoteId` — echo back on submit to lock pricing
- `from` / `to` — directional legs, each with `asset` (`"target"` or `"contra"`), `amount`, and `notional`
- `fees.estimatedFeeNotional` — total fee in USD notional
- `evm` *(EVM quotes)* — signing payloads:
  - `evm.orderTypedData` — EIP-712 payload to sign
  - `evm.permitTypedData` *(optional, Permit2)* — typed-data payload to sign
  - `evm.approveTx` *(optional)* — token-allowance transaction to submit onchain

### 2. Sign the payload

**Token allowance (one-time per token).** If the quote returns `evm.approveTx`, submit it onchain first to grant the settlement contract allowance. Once landed, future orders for the same token skip this step.

**Permit2 (per-order).** If the quote returns `evm.permitTypedData`, sign it the same way as `evm.orderTypedData`. On submit, pass the signature as `evmPermitSignature` and echo the payload back as `evmPermitTypedData`.

**Order signature.** `evm.orderTypedData` is a JSON-stringified EIP-712 payload. Parse it and sign with `signTypedData`. On submit, pass the signature as `userSignature` and echo the payload back as `evmOrderTypedData`. Example with viem:

```ts
import { privateKeyToAccount } from "viem/accounts";

const account = privateKeyToAccount("0x...");
const { domain, types, primaryType, message } = JSON.parse(quote.evm.orderTypedData);
const userSignature = await account.signTypedData({ domain, types, primaryType, message });
```

### 3. Submit the order

`POST /order` — executes the swap. Pass the same fields as the quote plus `funderAddress` (wallet funding the swap), `quoteId` from step 1, `userSignature` from step 2, and `evmOrderTypedData` (echo of `quote.evm.orderTypedData`). If Permit2 was used, also include `evmPermitTypedData` (echo of `quote.evm.permitTypedData`) and `evmPermitSignature`. The EVM deadline is encoded inside `evmOrderTypedData` — no separate deadline field needed.

Returns an `orderId`.

## Supported chains

`arbitrum`, `avalanche`, `base`, `blast`, `bsc`, `ethereum`, `optimism`, `polygon`, `solana`, `hyperevm`, `plasma`, `monad`

## Errors

All errors return:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid request",
    "details": {}
  }
}
```

HTTP status codes: `400` (bad request), `401` (invalid API key), `404` (not found), `429` (rate limited — see [Rate Limits](/rate-limits)), `500`/`503`/`504` (server error).
